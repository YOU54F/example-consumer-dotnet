# name: pact_verifier_cli
# https://github.com/pact-foundation/pact-reference/tree/master/rust/pact_verifier_cli
parameters:
  - name: PACT_VERIFIER_CLI_VERSION
    default: "" 

steps:
- script: |
    curl -sSL https://raw.githubusercontent.com/YOU54F/pact-reference/refs/heads/master/scripts/install-pact_verifier_cli.sh | bash
    echo '##vso[task.prependpath]/home/vsts/.pact/bin'
  displayName: Setup Pact Verifier CLI